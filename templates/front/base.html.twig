<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Popina{% endblock %}</title>
        {% block stylesheets %}{% endblock %}

        {{ encore_entry_link_tags('app') }}
        {{ encore_entry_script_tags('app') }}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark">
            {# <img id="iconHeader" src="/icon.png" alt="logo de l'application"/> #}
            <a class="navbar-brand homeButtonHeader" href="{{ path('home') }}">Popina</a>
            <button class="navbar-toggler" type="butston" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav mr-auto">
                    {% if app.user %}
                        <li class="nav-item"><span class="nav-link">Connecté en tant que : {{ app.user.username }}</span></li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('logout') }}">{{ 'global.logout'|trans }}</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </nav>

        <div class="container">
            {% block content %}
            {% endblock %}
        </div>
        {% block javascripts %}{% endblock %}
    </body>

    <footer>
        <span>La Popina - 2019 - Lola Gauchet, Peter Brejassou</span>
    </footer>
</html>
