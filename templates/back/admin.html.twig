{% extends 'front/base.html.Twig' %}

{% block content %}
    <h1>Administration</h1>

    <h3>Liste des restaurants</h3>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Nom</th>
            </tr>
        </thead>
            <tbody>
            {% for restaurant in restaurants %}
                <tr>
                    <td>{{ restaurant.id }}</td>
                    <td>{{ restaurant.nom }}</td>
                    <td><a href="{{ path('restaurant_detail_admin', {id: restaurant.id}) }}">Voir les détails</a></td>
                    <td><a href="{{ path('update_restaurant', {id: restaurant.id}) }}">Modifier</a></td>
                    <td><a href="{{ path('remove_restaurant', {id: restaurant.id}) }}">Supprimer</a></td>
                </tr>
            {% endfor %}
            </tbody>
    </table>

    <a href="{{ path('add_restaurant') }}">Ajouter un restaurant</a>
{% endblock %}
